# C# Bindings

In this sample you'll see how to initialize and host a C# library where you can interact with the paradigm rendering library. This is useful if you wish to write your application code in C# and use the rendering library to render your app.